//! Penguin Wave CLI.
//!
//! Covers every method the UI can call, so the daemon is fully usable with no
//! GUI installed.
//!
//! Filled in at P4. See `claudedocs/workflow_daemon_split.md`.

fn main() {
    eprintln!("penguinwave-cli: not implemented yet (P4)");
    std::process::exit(1);
}
